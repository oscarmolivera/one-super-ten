<%= turbo_stream.replace "matches_table",
      partial: "season_teams/matches/match_table",
      locals: { tournament_data: @tournament_data } %>

<%= turbo_stream.append "modal_control" do %>
  <turbo-frame>
    <script>
      window.closeMatchModal && window.closeMatchModal()
    </script>
  </turbo-frame>
<% end %>